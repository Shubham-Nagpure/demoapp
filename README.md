
  # Set Header and Page (Copy)

  This is a code bundle for Set Header and Page (Copy). The original project is available at https://www.figma.com/design/zuXAkiSZ3sfxz81vZkQv8x/Set-Header-and-Page--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  